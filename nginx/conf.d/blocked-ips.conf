# =====================================
# Known malicious IPs & Networks
# =====================================

# Block known malicious IPs
deny 185.177.72.108;

# Optional: Block entire malicious subnets
# deny 185.177.72.0/24;

# Optional: Allow only specific networks (uncomment if needed)
# allow 192.168.1.0/24;     # Your local network
# deny all;                 # Block everything else (CAREFUL - this blocks all other IPs!)

# Example: Geo-blocking (requires GeoIP module)
# geo $blocked_country {
#     default 0;
#     CN 1;  # China
#     RU 1;  # Russia
# }
