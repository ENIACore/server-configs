## Jackett configuration

1. Configure admin password
2. Copy the API key from the top-right of the Jackett web UI and run `jackett-configure` to wire it into qBittorrent's search plugin
3. Add custom url domain to Base URL Override
4. Add JACKETT_DOWNLOADS_DIR to blackhole directory
5. Add flaresolverr url
6. Add omdb api key
